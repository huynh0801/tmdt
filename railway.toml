[build]
builder = "NIXPACKS"
buildCommand = "bash build.sh"

[deploy]
startCommand = "java $JAVA_OPTS -jar build/libs/webapp-1.0-SNAPSHOT.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
